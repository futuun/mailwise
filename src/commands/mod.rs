//! CLI command implementations. One module per `mailwise <subcommand>`;
//! `main.rs` only does argument parsing + dispatch.

pub mod agent;
pub mod config;
pub mod index;
pub mod open;
pub mod search;
